#include <stdio.h>
#include <stdlib.h>

int main(){
  printf("hello world, getting git set up");

  return 0;
}

/* git add -all // adds all files to git in the current folder
   git commit -a  //opens vim to add a commit message and brings to git
   git push  //pushes your work to git
*/



/*put the assignment in the A1 git repository*/
//make a seperate .h for all functions